Index: squid-3.4.6/src/FwdState.cc
===================================================================
--- squid-3.4.6.orig/src/FwdState.cc
+++ squid-3.4.6/src/FwdState.cc
@@ -1101,13 +1101,13 @@ FwdState::connectStart()
     if (ftimeout < ctimeout)
         ctimeout = ftimeout;
 
-    if (serverDestinations[0]->getPeer() && request->flags.sslBumped) {
-        debugs(50, 4, "fwdConnectStart: Ssl bumped connections through parent proxy are not allowed");
-        ErrorState *anErr = new ErrorState(ERR_CANNOT_FORWARD, Http::scServiceUnavailable, request);
-        fail(anErr);
-        self = NULL; // refcounted
-        return;
-    }
+    //if (serverDestinations[0]->getPeer() && request->flags.sslBumped) {
+    //    debugs(50, 4, "fwdConnectStart: Ssl bumped connections through parent proxy are not allowed");
+    //    ErrorState *anErr = new ErrorState(ERR_CANNOT_FORWARD, Http::scServiceUnavailable, request);
+    //    fail(anErr);
+    //    self = NULL; // refcounted
+    //    return;
+    //}
 
     request->flags.pinned = false; // XXX: what if the ConnStateData set this to flag existing credentials?
     // XXX: answer: the peer selection *should* catch it and give us only the pinned peer. so we reverse the =0 step below.
